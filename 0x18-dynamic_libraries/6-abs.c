#include "holberton.h"

/**
 * _abs - valor entero
 * @r: Number to evaluate.
 * Return: Devuelve r cuando es valor absoluto positivo,
 */
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}
}
